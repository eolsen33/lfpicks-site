# lfpicks.ericolsen.studio — deploy output

Generated by `_build/build-lf.sh` in the private `eolsen33/nfl-pick-pool` repo. Do not edit here;
edit `site/` there, rebuild, and push this folder. Hostinger Git deploy pulls `main` of this repo
into the `lfpicks.ericolsen.studio` docroot (root directory left blank). The API lives on the VPS at
https://picks.ericolsen.studio/api/pool.
